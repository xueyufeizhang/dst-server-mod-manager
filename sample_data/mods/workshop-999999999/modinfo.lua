-- Deliberately broken modinfo.lua (unterminated string) so you can see how
-- the web UI reports a mod whose modinfo failed to parse.
name = "Broken Mod
description = this file has a syntax error on purpose
